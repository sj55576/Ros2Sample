name: ROS 2 CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  lyrical:
    name: Ubuntu 24.04 / ROS 2 Lyrical
    runs-on: ubuntu-24.04
    env:
      ROS_DISTRO: lyrical
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up ROS 2 apt repository
        uses: ros-tooling/setup-ros@v0.7
        with:
          required-ros-distributions: lyrical

      - name: Install developer tools
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3-colcon-common-extensions \
            python3-rosdep \
            python3-vcstool

      - name: Import vcs dependencies
        run: |
          mkdir -p src
          vcs import src < ros2.repos

      - name: Install ROS dependencies
        run: ./scripts/rosdep-install.sh lyrical

      - name: Build
        run: |
          export AMENT_TRACE_SETUP_FILES="${AMENT_TRACE_SETUP_FILES:-}"
          source /opt/ros/lyrical/setup.bash
          ./scripts/build.sh

      - name: Lint
        run: |
          export AMENT_TRACE_SETUP_FILES="${AMENT_TRACE_SETUP_FILES:-}"
          source /opt/ros/lyrical/setup.bash
          ./scripts/lint.sh

      - name: Test
        run: |
          export AMENT_TRACE_SETUP_FILES="${AMENT_TRACE_SETUP_FILES:-}"
          source /opt/ros/lyrical/setup.bash
          colcon test --event-handlers console_direct+
          colcon test-result --verbose

  jazzy:
    name: Ubuntu 24.04 / ROS 2 Jazzy
    runs-on: ubuntu-24.04
    env:
      ROS_DISTRO: jazzy
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up ROS 2 apt repository
        uses: ros-tooling/setup-ros@v0.7
        with:
          required-ros-distributions: jazzy

      - name: Install developer tools
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3-colcon-common-extensions \
            python3-rosdep \
            python3-vcstool

      - name: Import vcs dependencies
        run: |
          mkdir -p src
          vcs import src < ros2.repos

      - name: Install ROS dependencies
        run: ./scripts/rosdep-install.sh jazzy

      - name: Build
        run: |
          export AMENT_TRACE_SETUP_FILES="${AMENT_TRACE_SETUP_FILES:-}"
          source /opt/ros/jazzy/setup.bash
          ./scripts/build.sh

      - name: Lint
        run: |
          export AMENT_TRACE_SETUP_FILES="${AMENT_TRACE_SETUP_FILES:-}"
          source /opt/ros/jazzy/setup.bash
          ./scripts/lint.sh

      - name: Test
        run: |
          export AMENT_TRACE_SETUP_FILES="${AMENT_TRACE_SETUP_FILES:-}"
          source /opt/ros/jazzy/setup.bash
          colcon test --event-handlers console_direct+
          colcon test-result --verbose
